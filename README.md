# Trie - Java

To start use `javac TrieStart.java && java TrieStart` in directory with code

You can insert only words on english without spaces and special characters

Trie does not saving after exiting program
